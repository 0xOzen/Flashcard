# FlashCard-E / WortSchatz Web App Runbook

## Local orientation

1. Read `AGENTS.md`, `project.yaml`, `PROJECT.md`, `README.md`, `ops/STATUS.md`, and `ops/TODO.md`.
2. Install dependencies only if needed for app work: `npm install`.
3. Run local development using the commands documented in `README.md`.

## Approval boundary

Do not deploy, push, PR, publish, connect credentials, or use external services without Ali approval.
